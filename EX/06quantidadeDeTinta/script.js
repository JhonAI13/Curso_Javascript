let altura = 2;
let largura = 6;

let baldes = (largura * altura) / 2;
let area = largura * altura;
// ((l * l2) / 2,(l * l2)))
console.log("Você vai precisar de", baldes, "baldes,para pintar", area ,"metros cubicos de parede." );
