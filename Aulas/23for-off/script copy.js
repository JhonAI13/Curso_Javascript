// Escreva uma função que usa 2 números e retorna o maior entre eles

const numeros = [8 , 7];

for (let local of numeros) {
  let int = 0;
  if(int > local){
    let int = local;
  }
  console.log(int);
}